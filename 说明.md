# LM 局域网聊天

手机/电脑通过浏览器访问 LM Studio 本地大模型

## 首次使用

1. 安装 [Node.js](https://nodejs.org)（18 以上版本）
2. 安装 [LM Studio](https://lmstudio.ai)，加载一个模型，开启开发者模式的本地服务（端口 1234）
3. 双击 `启动.bat`
4. 浏览器打开终端显示的地址

## 修改配置

用记事本打开 `配置.json`：

```
port        → 网页端口（默认 8080）
passcode    → 登录密码（改掉！）
temperature → 温度参数（0~2，越高越随机）
maxTokens   → 每次回复最大长度
maxHistory  → 保留多少轮对话上下文
```

改密码那行：`"passcode": "你的新密码"`

## 手机访问

和电脑连同一个 WiFi，浏览器打开终端显示的局域网地址。
iPhone 用 Safari、安卓用 Chrome 打开后，可「添加到主屏幕」。

## 文件说明

| 文件 | 作用 |
|------|------|
| `启动.bat` | 双击启动 |
| `server.js` | 服务端程序 |
| `聊天界面.html` | 网页聊天界面 |
| `配置.json` | 可修改的配置文件 |

## 分享给别人

把整个文件夹（删掉 node_modules）压缩发给对方。
对方解压后双击 `启动.bat`，会自动安装依赖。
